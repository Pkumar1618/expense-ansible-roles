[defaults]

inventory=inventory.ini
remote_user=ec2-user
ask_vault_pass=true
forks=10 # using this it connected at a time 10 servers. this is works at task level.
private_key_file=/home/ec2-user/ansible.pem # here we are using the kay-pair with connect to server.